name: Build RPM Package

on:
  workflow_call:

jobs:
  build:
    name: Build RPM Package
    runs-on: ubuntu-latest
    container:
      image: fedora:latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          dnf install -y \
            gcc gcc-c++ make cmake pkg-config ruby-devel \
            qt5-qtbase-devel \
            systemd-devel parted-devel libarchive-devel \
            libcdio-devel openssl-devel libcurl-devel libudev-devel

      - name: Install FPM
        run: gem install fpm

      - name: Build RPM package
        run: bash scripts/build-rpm.sh "${{ github.ref_name }}"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: rpm
          path: "winafi-*.rpm"
          retention-days: 1
